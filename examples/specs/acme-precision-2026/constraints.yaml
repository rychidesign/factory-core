budget:
  max_tokens_total: 5000000
  max_dollar_cost: 10.00
  max_iterations: 300
  max_duration_hours: 48
  max_consecutive_failures: 5

deadlines:
  intake_complete: "2026-04-27"
  design_approval: "2026-05-12"
  staging_review: "2026-05-22"
  production_launch: "2026-05-29"

accessibility:
  min_wcag_level: AA
  additional_requirements:
    - "Plně použitelné na klávesnici (klient používá hodně Tab v interním systému)."

performance_targets:
  min_lighthouse_mobile: 90
  min_lighthouse_desktop: 95
  max_largest_contentful_paint_ms: 2500
  max_total_blocking_time_ms: 200
  max_cumulative_layout_shift: 0.1

compliance: ["GDPR", "WCAG-AA"]
data_residency: EU

do_not_use:
  - "Google Analytics (klient preferuje Plausible kvůli GDPR)."
  - "react-slick (a11y problémy)."
  - "Hero video na homepage (mobile data)."

intellectual_property:
  code_ownership: client
  design_ownership: client
  ai_disclosure_to_client: true

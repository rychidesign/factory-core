# Sanity hosted CMS
# Find these in https://sanity.io/manage after creating a project.
# Without these, the site renders with mock placeholder content.
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_VERSION=2025-01-01

# Optional: only needed when fetching draft / private content.
SANITY_READ_TOKEN=
